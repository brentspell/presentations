# Generated by Buckaroo - https://buckaroo.pm
include_defs('//BUCKAROO_DEPS')

cxx_library(
  name = 'dhke',
  header_namespace = 'dhke',
  srcs = glob([
    'dhke/src/**/*.cpp',
  ]),
  headers = subdir_glob([ # private include files
    ('dhke/detail', '**/*.h'), # they are only accesible inside the library
    ('dhke/detail', '**/*.hpp'),
  ]),
  exported_headers = subdir_glob([ # public include files
    ('dhke/include', '**/*.h'), # those will be exported
    ('dhke/include', '**/*.hpp'), # and accessible via <dhke/header.h>
  ]),
  deps = BUCKAROO_DEPS,
  visibility = ['PUBLIC']
)

cxx_binary(
  name = 'main',
  srcs = ['dhke/apps/main.c'],
  deps = [':dhke'],
  visibility = ['PUBLIC']
)